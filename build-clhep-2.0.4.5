#!/bin/bash

version=2.0.4.5
sourceDir=${HOME}/programs/clhep/$version
installDir=/opt/clhep/$version

if [ -d $installDir ]
then
   echo "Removing the previous installation"
   rm -r $installDir
fi

cd $sourceDir
./configure --prefix=$installDir && make clean && make -j24 install
