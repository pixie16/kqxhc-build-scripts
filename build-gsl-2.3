#!/bin/bash

version=2.3
sourceDir=${HOME}/programs/gsl/$version
installDir=/opt/gsl/$version

if [ -d $installDir ]
then
   echo "Removing the previous installation"
   rm -r $installDir
fi

cd $sourceDir
./configure --prefix=$installDir && make clean && make -j24 install
